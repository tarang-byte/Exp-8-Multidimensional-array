#include <iostream>
using namespace std;

int main() {
    int rows, cols;

    cout << "Enter number of rows: ";
    cin >> rows;
    cout << "Enter number of columns: ";
    cin >> cols;

    int matrix1[100][100];  int sum=0;

    cout << "\nEnter elements of first matrix:\n";
    for (int i = 0; i < rows; ++i) {
        for (int j = 0; j < cols; ++j) {
            cout << "Element at (" << i + 1 << "," << j + 1 << "): ";
            cin >> matrix1[i][j];
        }
    }

    for (int i = 0; i < rows; ++i) {
        for (int j = 0; j < cols; ++j) {
            if(i==j)
            sum+= matrix1[i][j];
        }
    }

            cout << sum << " ";
    

    return 0;
}

Enter number of rows: 3
Enter number of columns: 3

Enter elements of first matrix:
Element at (1,1): 3
Element at (1,2): 4
Element at (1,3): 5
Element at (2,1): 6
Element at (2,2): 7
Element at (2,3): 8
Element at (3,1): 9
Element at (3,2): 10
Element at (3,3): 11
21 

=== Code Execution Successful ===
